<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.enciphermessenger">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App">
        <activity
            android:name="com.android.enciphermessenger.activities.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.android.enciphermessenger.activities.CredentialsActivity" />
        <activity android:name="com.android.enciphermessenger.activities.VerifyPhoneActivity" />
        <activity android:name="com.android.enciphermessenger.activities.LoginActivity" />
        <activity android:name="com.android.enciphermessenger.activities.ChatActivity" />
        <activity android:name="com.android.enciphermessenger.activities.MainActivity" />
    </application>

</manifest>